import { Stack } from "@mui/material";

export function TokenBoard({ component }: { component: any }) {
  return (
    <Stack>
      {component}
      {component}
      {component}
      {component}
    </Stack>
  );
}
